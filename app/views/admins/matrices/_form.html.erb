
<%= simple_form_for [:admins, @matrix] do |f| %>

  <%= f.input :name %>
  

  <div class="card-footer text-right">
    <div class="d-flex">
      <%= link_to "Voltar", admins_matrices_path, class: 'btn btn-link' %>
      <%= f.button :submit, class: 'btn btn-primary ml-auto'%>
    </div>
  </div>

<% end %>
